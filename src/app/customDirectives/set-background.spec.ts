import { SetBackground } from './set-background';

describe('SetBackground', () => {
  it('should create an instance', () => {
    const directive = new SetBackground();
    expect(directive).toBeTruthy();
  });
});
